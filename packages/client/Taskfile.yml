version: '3'

tasks:
  build:
    desc: build the client to wasm
    cmds:
      - trunk clean
      - trunk build

  build:release:
    desc: build the client to wasm in release mode
    cmds:
      - trunk clean
      - trunk build --release

  run:dev:
    desc: run client in dev mode
    deps:
      - task: serve
      - task: gen:css:watch

  serve:
    cmds:
      - trunk serve --open

  gen:css:
    env:
      NODE_ENV: production
    sources:
      - ./src/**/*.rs
      - ./index.html
      - ./src/**/*.html
      - ./src/**/*.css
    cmds:
      - tailwindcss -c ./tailwind.config.js -o ./src/css/tailwind.css --minify

  gen:css:watch:
    cmds:
      - task --watch "gen:css"